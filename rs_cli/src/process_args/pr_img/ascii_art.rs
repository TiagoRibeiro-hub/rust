use crate::{response::Response, process_img::Image};

#[allow(unused_variables, dead_code, unused_mut)]
pub fn process(
    third_op: &str,
    args: &Vec<String>,
    mut image: Image,
) -> Response {
    todo!()
}